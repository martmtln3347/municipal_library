{# templates/book/show.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Détails du livre{% endblock %}

{% block body %}
    <h1 class="mb-4">📖 Détails du livre</h1>

    <table class="table table-bordered align-middle">
        <tbody>
        <tr>
            <th style="width: 240px;">Titre</th>
            <td>{{ book.title }}</td>
        </tr>
        <tr>
            <th>Résumé</th>
            <td>{{ book.summary|default('—')|nl2br }}</td>
        </tr>
        <tr>
            <th>Année de publication</th>
            <td>{{ book.publicationYear|default('—') }}</td>
        </tr>
        <tr>
            <th>Ajouté par</th>
            <td>
                {% if book.user %}
                    {{ book.user.firstName }} {{ book.user.lastName }}
                {% else %}
                    <em>Inconnu</em>
                {% endif %}
            </td>
        </tr>
        <tr>
            <th>Date d’ajout</th>
            <td>{{ book.createdAt ? book.createdAt|date('d/m/Y H:i') : '—' }}</td>
        </tr>
        <tr>
            <th>Dernière mise à jour</th>
            <td>{{ book.updatedAt ? book.updatedAt|date('d/m/Y H:i') : '—' }}</td>
        </tr>
        </tbody>
    </table>

    <div class="d-flex gap-2">
        <a href="{{ path('app_book_index') }}" class="btn btn-secondary">⬅️ Retour à la liste</a>
        <a href="{{ path('app_book_edit', { id: book.id }) }}" class="btn btn-primary">✏️ Modifier</a>

        {# Include déplacé ici, dans le block, avec book défini #}
        {% include 'book/_delete_form.html.twig' with { book: book } %}
    </div>
{% endblock %}
